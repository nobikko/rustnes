[package]
name = "nes-wasm"
version = "0.1.0"
edition = "2021"
description = "WASM wrapper for NES emulator"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
nes-core = { path = "../nes-core" }
wasm-bindgen = "0.2"

[dev-dependencies]
wasm-bindgen-test = "0.3"